apiVersion: v1
kind: Secret
metadata:
  name: discord-mail-secret
type: Opaque
data:
  # Replace this with your base64 encoded Discord webhook URL
  # echo -n "your-webhook-url" | base64
  DISCORD_WEBHOOK_URL: "YOUR_BASE64_ENCODED_WEBHOOK_URL" 